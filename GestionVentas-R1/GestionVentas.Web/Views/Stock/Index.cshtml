@using GestionVentas.Web.Enum

@model List<GestionVentas.Web.Models.ViewModels.Stock.StockViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>this is stock view perrrraaa</h3>

<div class="container-fluid">
    <div class="container-fluid">
        <div class="row justify-content-between">
            <div class="col-lg-7 col-md-7 col-sm-12">
                <ul class="nav ">
                    <li class="nav-item ">
                        <a class="nav-link  btn-success" asp-area="" asp-controller="Modelos" asp-action="form" asp-route-accionCRUD=@AccionesCRUD.AGREGAR><i class="fa fa-plus" aria-hidden="true"></i>Agregar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn-dark" href="#"><i class="fa fa-file-excel-o" aria-hidden="true"></i>Exportar</a>
                    </li>
                </ul>
            </div>
            <div class="col-lg-5 col-md-5 col-sm-12 d-lg-inline-flex d-md-inline-flex justify-content-end ">
                <form class="form-inline" asp-action="buscar" asp-controller="modelos" method="get">

                    @* name=[nameParameter from method] --> modelos/buscar?p_query= [valueInput]   *@
                    <input class="form-control form-control-sm" placeholder="Buscar..." name="p_query">
                    <button class="form-control form-control-sm btn btn-info " type="submit"><i class="fa fa-search" aria-hidden="true"></i>Buscar</button>
                </form>
            </div>
        </div>
    </div>
    <div class="container-fluid">
        <h6 class="m-2">Registros: </h6>
        <div class="container-fluid overflow-auto " style="max-height:300px;">
            <table class="table table-light table-hover  border-dark table-sm small">
                <thead class=" thead-dark">
                    <tr>
                        <th>Acciones</th>
                        <th>#</th>
                        <th>Articulo Descripcion</th>
                        <th>Cantidad</th>

                    </tr>
                </thead>
                <tbody class="table-bordered">
                    @{
                        int contRegistros = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <th>
                                    <a asp-action="eliminar" asp-controller="stock" asp-route-id=@item.Id class="text-decoration-none text-danger"><i class="fa fa-trash fa-2x" aria-hidden="true"></i></a>
                                    <a asp-action="form" asp-controller="stock" asp-route-accionCRUD=@AccionesCRUD.MODIFICAR asp-route-id=@item.Id class="text-decoration-none"><i class="fa fa-pencil fa-2x" aria-hidden="true"></i></a>
                                    <a asp-action="detalle" asp-controller="stock" asp-route-id=@item.Id class="text-decoration-none text-dark"><i class="fa fa-info fa-2x" aria-hidden="true"></i></a>
                                </th>
                                <th>@contRegistros</th>
                                <td>@item.ArticuloDescripcion</td>
                                <td>@item.Cantidad</td>
                            </tr>


                            contRegistros = contRegistros + 1;
                        }

                    }
                </tbody>
            </table>
        </div>

    </div>
</div>

